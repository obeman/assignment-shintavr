# assignment-shintavr

Install dependencies
### `npm install`

Then add your firebase config in .env for use at
### `firebase.js`

Then run the app
### `node app.js`

App is running in localhost:5000
